<main>
    <h2>home</h2>

    {{#each books}}
        <div class="book">
            <h3>Título: {{this.title}}</h3>
            <p>Quantidade de páginas: {{this.pageqty}}</p>
        </div>
    {{/each}}
</main>